{
  "name": "@wallets-e2e/example-spike",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The story's proving test: launches the real Leather extension, unlocks it via the devnet fixture wallet, and records video.",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "test": "playwright test"
  },
  "dependencies": {
    "@playwright/test": "^1.47.0",
    "@wallets-e2e/core": "workspace:*",
    "@wallets-e2e/leather": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.7.5",
    "@types/chrome": "^0.2.7"
  }
}
