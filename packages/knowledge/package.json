{
  "name": "@wallets-e2e/knowledge",
  "version": "0.1.5",
  "type": "module",
  "description": "Agent-facing guides for driving real wallet extensions in Playwright. One source of truth, consumed by the Claude skill and by the MCP server so any agent gets the same knowledge.",
  "keywords": ["wallet", "e2e", "playwright", "agent", "mcp", "claude", "skill", "documentation"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yehia67/e2e-wallets.git",
    "directory": "packages/knowledge"
  },
  "bugs": { "url": "https://github.com/yehia67/e2e-wallets/issues" },
  "homepage": "https://github.com/yehia67/e2e-wallets#readme",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist", "src", "SKILL.md", "references", "!src/**/*.test.ts"],
  "engines": { "node": ">=22.18" },
  "publishConfig": { "access": "public" },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test"
  },
  "devDependencies": { "@types/node": "^22.7.5", "typescript": "^5.6.3" }
}
