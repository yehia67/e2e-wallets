{
  "name": "@stacks-wallet/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared context-launch and extension-ID-resolution machinery every wallet driver in this monorepo builds on.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@playwright/test": "^1.47.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "@types/chrome": "^0.2.7",
    "typescript": "^5.6.3"
  }
}
