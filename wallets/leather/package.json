{
  "name": "@stacks-wallet/leather",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "WalletDriver adapter for the real Leather browser extension, built from source (leather-io/extension).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./fixtures/devnet-wallet.js": "./fixtures/devnet-wallet.ts"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "build:extension": "bash ./scripts/build-extension.sh"
  },
  "dependencies": {
    "@playwright/test": "^1.47.0",
    "@stacks-wallet/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.7.5",
    "@types/chrome": "^0.2.7"
  }
}
